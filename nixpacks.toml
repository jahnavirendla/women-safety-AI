[phases.setup]
nixPkgs = ["python3", "python311Packages.pip", "gcc", "portaudio", "libGL", "libGLU", "alsa-lib"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 main.py"